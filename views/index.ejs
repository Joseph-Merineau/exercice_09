<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <link href="css/style.css" rel="stylesheet">
  <title>MERINEAU Mon Application</title>
</head>
<body>
  Node.js, Express et MongoDB.
  <button id='update'>Mise à jour téléphone</button>
    <form action="/adresse" method="POST">
    <input type="text" placeholder="Preom" name="prenom">
    <input type="text" placeholder="Nom" name="nom">
    <input type="text" placeholder="Telephone" name="telephone">
    <input type="text" placeholder="Ville" name="ville">
    <input type="text" placeholder="Code Postal" name="cp">
    <input type="text" placeholder="Groupe Sanguin" name="gs">
    <button type="submit">Submit</button>
  </form>

  <ul class="adresse">
  <% for(var i=0; i<adresse.length; i++) {%>
    <li class="client">
      <span><%= adresse[i].prenom %></span>
      <span><%= adresse[i].nom %></span>
      <span><%= adresse[i].telephone %></span>
      <span><%= adresse[i].ville %></span>
      <span><%= adresse[i].cp %></span>
      <span><%= adresse[i].gs %></span>
    </li>
  <% } %>
  </ul>
  

</body>
</html>